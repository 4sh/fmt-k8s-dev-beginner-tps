apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: ingress
  namespace: 4sh-formation-@ME
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/proxy-body-size: "20M"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - "4sh-formation-@ME.learning.quatre.app"
      secretName: my-training-@ME-cert
  rules:
    - host: 4sh-formation-@ME.learning.quatre.app
      http:
        paths:
          - path: /api/
            backend:
              serviceName: app-srv
              servicePort: 80
          - path: /
            backend:
              serviceName: app-ui
              servicePort: 80
---
